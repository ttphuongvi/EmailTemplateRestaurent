import TableCell from "../../atoms/TableCell";
import { styled } from "@mui/material/styles";

const TableCellStyles = styled(TableCell)({
  padding: "none",
  borderBottom: "none",
});

export default TableCellStyles;
